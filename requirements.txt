# Verdict Watch V7 — Requirements
# Google Enterprise Edition

# ── Core API & Server
fastapi
uvicorn[standard]
pydantic
pydantic-settings
httpx
python-dotenv

# ── UI
streamlit>=1.32.0

# ── Database
sqlalchemy

# ── AI — Primary pipeline
groq

# ── Charts & Data
plotly
pandas

# ── File parsing
PyMuPDF          # PDF extraction (pip name: PyMuPDF → import fitz)
python-docx      # DOCX extractions