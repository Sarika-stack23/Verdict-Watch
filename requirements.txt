# Verdict Watch V14 — Requirements
# Dual AI: Gemini (primary) + Groq (fallback)

# ── Core API & Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0
python-dotenv>=1.0.0

# ── UI
streamlit>=1.35.0

# ── Database
sqlalchemy>=2.0.0

# ── AI — PRIMARY (Google Gemini) ─────────────────────
google-generativeai>=0.7.0

# ── AI — FALLBACK (Groq / Llama 3.3 70B) ────────────
groq>=0.9.0

# ── Charts & Data
plotly>=5.22.0
pandas>=2.2.0

# ── File parsing
PyMuPDF          # PDF extraction  (import fitz)
python-docx      # DOCX extractions