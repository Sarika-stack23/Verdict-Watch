# Verdict Watch V20 — Requirements
# 8-step AI Governance Edition

# ── Core API & Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
httpx>=0.27.0
python-dotenv>=1.0.0

# ── UI
streamlit>=1.35.0

# ── Database
sqlalchemy>=2.0.0

# ── AI — GOVERNANCE LAYER (Vertex AI — Steps 4+5)
google-cloud-aiplatform>=1.43.0

# ── AI — PRIMARY (Google Gemini — Steps 0–3, 6–7)
google-generativeai>=0.8.0

# ── AI — FALLBACK (Groq)
groq>=0.9.0

# ── Retry / resilience (V20 — replaces time.sleep)
tenacity>=8.2.0

# ── Charts & Data
plotly>=5.22.0
pandas>=2.2.0

# ── File parsing
PyMuPDF           # PDF extraction  (import fitz)
python-docx       # DOCX extraction

# ── Export
openpyxl>=3.1.0   # Excel export
weasyprint>=60.0  # PDF report generation (V20)

# ── Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0